@model IEnumerable<Web_xemphim.Data.ReleaseYear>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.YearRelease)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.YearRelease)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReleaseYearId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ReleaseYearId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ReleaseYearId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
